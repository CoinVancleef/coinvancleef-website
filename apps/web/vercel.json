{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && rm -rf node_modules && NODE_ENV=development pnpm install -w --include=dev && NODE_ENV=development npm run build -- --filter=web...",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "release": true
    }
  }
}
