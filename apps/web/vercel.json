{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && cd apps/web && npm install --no-optional --no-package-lock --ignore-scripts && npm run build",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "release": true
    }
  }
}
