// This file contains global test setup code

// Use proper type declarations instead of direct import
// No need to import @types/jest directly, Jest automatically adds its globals
import type { jest } from '@jest/globals';

// Add any other global test setup here
